"use client";

import { useFormContext } from "react-hook-form";

interface Props {
  name: string;
}

export function MessageTemplateComponentParameterMedia() {
  const { control } = useFormContext();

  return <></>;
}
